# Web streaming App

- React
- Json Server
- Redux

### Json Server (Api)
- cd `api`
- Run `yarn intsall`
- Run `yarn start`

### CLient (Api)
- cd `client`
- Run `yarn intsall`
- Run `yarn start`

open app on localhost:3000